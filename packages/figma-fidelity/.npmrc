# Allow esbuild native builds (vitest / tooling).
# Kept here instead of a fake pnpm-workspace.yaml so this package
# is clearly a standalone npm project under packages/, not a workspace root.
allowBuilds.esbuild=true
