{
  "description": "Check Figma component registry drift after Claude Code edits component source.",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks/check-component-registry-drift.mjs\"",
            "timeout": 30,
            "statusMessage": "Checking Figma component registry drift"
          }
        ]
      }
    ]
  }
}
