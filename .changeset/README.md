# Changesets

Run `npm run changeset` for user-visible changes. Commit generated changeset files with code changes.

Release automation opens a version PR, synchronizes three Figloom manifests, validates bundle drift, and creates Git tags after merge.
