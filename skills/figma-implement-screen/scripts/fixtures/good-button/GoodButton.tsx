import { Button } from "#/components/ui/button";

// Positive pressure fixture for resolved design-system reuse.
export function GoodButton() {
  return (
    <Button size="lg" variant="filled" color="green">
      Click
    </Button>
  );
}
